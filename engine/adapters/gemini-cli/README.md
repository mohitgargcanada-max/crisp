# Gemini CLI Adapter

Use project instructions plus compact shell functions.

Project instruction:

```text
Use token-efficient-agent behavior: compact final answers, narrow file reads, grouped log/test failures, exact commands/errors/code preserved.
```

For terminal savings, use `adapters/terminal`.

If the CLI supports MCP in your environment, use the `adapters/generic-mcp` contract.
