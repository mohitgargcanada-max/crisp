# Memory Vault Templates

These files are templates for a local markdown memory vault.

Run:

```powershell
node <CRISP_HOME>\cli\tea.js vault init
```

The real default vault is `<CRISP_HOME>\memory-vault`, which is ignored by git so private memory is not pushed.
